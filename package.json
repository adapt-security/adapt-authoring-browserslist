{
  "name": "adapt-authoring-browserslist",
  "version": "1.3.0",
  "description": "Handles updating of browserslist for the framework building process",
  "homepage": "https://github.com/adapt-security/adapt-authoring-browserslist",
  "license": "GPL-3.0",
  "type": "module",
  "main": "index.js",
  "repository": "github:adapt-security/adapt-authoring-browserslist",
  "peerDependencies": {
    "adapt-authoring-adaptframework": "^1.9.3",
    "adapt-authoring-core": "^2.0.0"
  },
  "peerDependenciesMeta": {
    "adapt-authoring-adaptframework": {
      "optional": true
    },
    "adapt-authoring-core": {
      "optional": true
    }
  },
  "devDependencies": {
    "@semantic-release/git": "^10.0.1",
    "conventional-changelog-eslint": "^6.0.0",
    "semantic-release": "^25.0.2",
    "standard": "^17.1.0"
  },
  "release": {
    "plugins": [
      [
        "@semantic-release/commit-analyzer",
        {
          "preset": "eslint"
        }
      ],
      [
        "@semantic-release/release-notes-generator",
        {
          "preset": "eslint"
        }
      ],
      "@semantic-release/npm",
      "@semantic-release/github",
      [
        "@semantic-release/git",
        {
          "assets": [
            "package.json"
          ],
          "message": "Chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
        }
      ]
    ]
  },
  "scripts": {
    "test": "node --test 'tests/**/*.spec.js'"
  }
}
